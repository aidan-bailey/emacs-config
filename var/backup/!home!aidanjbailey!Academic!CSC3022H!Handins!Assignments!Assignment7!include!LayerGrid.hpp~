#ifndef LAYERGRID_HPP
#include <vector>

namespace BLYAID001 {

struct perceptron {
  bool State;
  std::vector<float> Weights;
};

class LayerGrid {
  std::vector<std::vector<perceptron>> perceptrons;

private:
};

}; // namespace BLYAID001
#endif
